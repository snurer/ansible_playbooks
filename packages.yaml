---
- name: Install vim, curl and wget
  hosts:
   - host1
   - host2
  tasks:
    - name: Install vim
      yum:
        name:
        - vim
        - curl
        - wget
        state: present
        